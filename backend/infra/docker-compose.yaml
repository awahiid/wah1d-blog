name: wah1d-blog-backend

version: "3.8"

services:
  postgres:
    image: postgres:16
    container_name: wah1d-blog-postgres
    restart: unless-stopped
    ports:
      - "5432:5432"
    env_file:
      - .env
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: